# Runtime deps for the AWS/Docker deployment.
# Same as requirements.txt MINUS tensorflow (the live-LSTM path is unused by the
# frontend — Tab 3 reads precomputed forecast CSVs) and the test-only packages.
# Dropping tensorflow shrinks the image ~10x and the build from ~15min to ~2min.
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
scipy==1.13.0
python-multipart==0.0.9
httpx==0.27.0
requests>=2.31.0          # Open-Meteo weather API calls
holidays>=0.46            # is_holiday feature (lazy ML path; tiny, kept for safety)
pulp==2.8.0               # MILP modeling + bundled CBC solver (no system deps)
