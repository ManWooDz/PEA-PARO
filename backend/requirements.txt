# Runtime dependencies (Vercel-friendly — no TensorFlow).
# The app serves precomputed forecasts from CSV, so no live ML inference is
# needed at runtime. To retrain the LSTM or regenerate forecast CSVs locally,
# also install requirements-ml.txt.
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pandas==2.2.2
numpy==1.26.4
python-multipart==0.0.9
httpx==0.27.0
requests>=2.31.0          # LINE push + Open-Meteo weather calls
pytest==8.2.0
pytest-asyncio==0.23.6
